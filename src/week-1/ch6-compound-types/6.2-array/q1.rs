
fn main() {
    // Fill the blank with proper array type
    let arr: [u8; 5] = [1, 2, 3, 4, 5]; // Array [Type; Size]

    // Modify the code below to make it work
    assert!(arr.len() == 5);

    println!("Success!");
}
